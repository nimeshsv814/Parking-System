apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: {{ .Values.namespace.name }}
  labels:
    Application: smart-parking
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - port: 4004
      targetPort: 4004
